flask>=3.0,<4
pyjwt>=2.8,<3
bcrypt>=4.1,<5
ultralytics>=8.2,<9
# ultralytics auto-installs pi-heif (HEIF/HEIC support) at runtime if missing;
# pin it so it's baked into the image instead of phoning home on first classify.
pi-heif>=0.21
pillow>=10.0,<12
